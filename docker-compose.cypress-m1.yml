version: "3.8"

services:
  cypress:
    image: cypress/included:10.4.0
    working_dir: "/test"
    volumes:
      - "./cypress:/test/cypress"
      - "./docker/cypress/cypress-docker.json:/test/cypress.json"
    command: cypress run
    network_mode: "host"
